# B6 Project Expansion Implementation Plan

1. Freeze the server-owned three-project mapping and validate the two exact B6 stage envelopes.
2. Generalize final-B5 remediation, lease, recovery, and stability only to the closed B6 project union while retaining M12's one-project runtime envelope unchanged.
3. Compose the B6 loopback runtime from final-B5 semantics under B6-owned state with exactly 13 tools and no new authority.
4. Freeze committed B6 bytes and compute the deterministic physical manifest using sorted `<file_sha256>  <path>\n` records.
5. Qualify `skill-fabric` through the fixed transaction/task recipe; prove exact13, hermes-os denial, effect policy, rollback/recovery, unchanged clean canonical state, and zero residue.
6. Seal Stage-1 certification against current B6/skill-fabric repository identity plus hashed live evidence; independently revalidate all current facts before Stage 2.
7. Only after Stage 1 is current and qualified, activate/qualify `hermes-os` independently, preserving rollback to qualified Stage 1.
8. Run focused + full regressions, typecheck/build, diff/secret scan, detached byte reproduction, independent/adversarial review, dogfood, rollback/re-activation proof, final seal, and post-seal reproduction.
9. Emit `HAIOS_DESKTOP_CONTROL_PLANE_R1_B6_PROJECT_EXPANSION_QUALIFIED` only when every gate is fresh/current and all original B6-owned live state is clean or verified-preserved.
